---
import site from "@/data/site.json";

export interface Props {
  author: string;
}

const { author } = Astro.props;
---

<footer class="my-10 font-basic font-light text-text">
  &copy; 2022 {author}. Built with {
    site.tooling.map((tool) => (
      <span>
        <a href={tool.url} class="text-pink hover:text-flamingo">
          {tool.name}
        </a>
        ,{" "}
      </span>
    ))
  }and 💛.
</footer>
