---
import siteConfig from '@/data/site-config'
import SectionHeading from './section-heading.astro'
import Block from './block.astro'
---

<Block>
  <SectionHeading>Where to find me</SectionHeading>
  <ul class="mt-2 grid gap-2 lg:grid-cols-2">
    {
      siteConfig.elsewhere.map((link) => (
        <li>
          <a
            href={link.url}
            class="text-md bg-tn-mantle font-rethink text-tn-purple1 hover:text-tn-purple0 block rounded-xl p-6 font-light"
            rel="me"
          >
            {link.icon} / {link.name}
          </a>
        </li>
      ))
    }
  </ul>
</Block>
